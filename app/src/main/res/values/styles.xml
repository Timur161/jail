<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="textarea">
        <item name="android:layout_marginStart">@dimen/big_space</item>
        <item name="android:layout_marginEnd">@dimen/big_space</item>
        <item name="android:layout_marginLeft">@dimen/big_space</item>
        <item name="android:layout_marginRight">@dimen/big_space</item>
        <item name="android:layout_marginTop">@dimen/big_space</item>
        <item name="android:layout_marginBottom">@dimen/big_space</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/dark</item>
    </style>
    <style name="Button">
        <item name="android:paddingStart">@dimen/small_space</item>
        <item name="android:paddingEnd">@dimen/small_space</item>
        <item name="android:paddingLeft">@dimen/small_space</item>
        <item name="android:paddingRight">@dimen/small_space</item>
        <item name="android:paddingTop">@dimen/small_space</item>
        <item name="android:paddingBottom">@dimen/small_space</item>
    </style>
    <style name="Button_size_restart">
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_height">50dp</item>
    </style>

</resources>
